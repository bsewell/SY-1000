{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command'); echo \"$cmd\" | grep -qE '(^|[[:space:]])qmake([[:space:]]|$)|(^make[[:space:]])' && echo '{\"decision\":\"block\",\"reason\":\"BLOCKED: Do not call qmake or make directly. Use ./build.sh instead.\"}' || true",
            "statusMessage": "Checking build command..."
          }
        ]
      }
    ]
  }
}
