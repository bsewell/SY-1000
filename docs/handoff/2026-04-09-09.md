# Handoff — 2026-04-09 Session 4

**Build**: 2026.04.09.72
**App**: `/Applications/SY-1000FloorBoard.app`

---

## Changes Made

### 1. BAL3 column position — moved right by 1 column

**File**: `src/floorBoard.cpp`

- Added `bal3xpos += 1` **after** the §L1 cap block (line ~1652) so the cap can't undo it.
- This gives BAL3 a full column of breathing room from its last input block (FV2), matching the visual weight BAL1 and BAL2 have.
- Before: FV2(679) → BAL3(711) = 32px gap (too tight). After: FV2(679) → BAL3(764) = 85px gap.

### 2. Removed BAL3 gapOverride

**File**: `src/floorBoard.cpp` (line ~2335)

- Changed `repositionBALOutputBlocks(33, -1, rowCenterBal3, true, bal3xpos, flowStep * 2 / 3)` → `repositionBALOutputBlocks(33, -1, rowCenterBal3, true, bal3xpos)` (uses standard flowStep gap).
- BAL3 output blocks (DD2, RV) now use the same spacing as BAL1/BAL2 output blocks.

### Current chain positions (build 72)

| Block | x | Gap from prev |
|-------|-----|---------------|
| FV2 | 679 | — |
| BAL3 | 764 | 85px (from FV2) |
| DD2 (DLY2) | 838 | 74px (from BAL3) |
| RV | 891 | 53px (standard) |
| DIV | 944 | 53px (standard) |
| MIX | 997 | 53px (standard) |

---

## Unverified Items

- **BAL3 visual position**: Diagnostics confirm BAL3 at x=764 (moved right by 53px from 711). User has NOT yet visually confirmed this looks correct.
- **BAL3 → DD2 gap (74px)**: Slightly wider than standard 53px flowStep. User previously complained about this gap being too long. May need further reduction.
- **Overall chain width**: The +1 column shift pushes the entire tail section (DIV/MIX/MST) right by ~53px. MST now at x=1156 (was ~1103). Check that the chain doesn't overflow the window on smaller displays.

---

## Known Issues / Regressions

- **BAL3 → DD2 gap is 74px** — still wider than the standard 53px between other blocks. The user has been iterating on this gap for several attempts. If it's still too wide, the `repositionBALOutputBlocks` call for BAL3 can take a negative `gapOverride` parameter, but the lambda currently only applies overrides when `> 0`. The lambda would need a small refactor (e.g., use a sentinel like `-1` for "use default" instead of `0`).
- **FV2 → BAL3 gap is 85px** — roughly 1.6 flowSteps. This is wider than any other single gap in the chain. User wanted "proper distance like BAL1" — BAL1→DD1 is 106px (2 flowSteps), so 85px may be acceptable. But if user wants exactly 1 flowStep (53px), the +1 column shift is too much and a fractional approach would be needed.

---

## Session History (from task tracker)

| # | Task | Status | Attempts |
|---|------|--------|----------|
| 12 | Move BAL3 right to proper column | In Progress | 2 (build 71: cap undid +1; build 72: +1 after cap) |

Previous sessions: see `memory/project_task_completion_tracking.md` for full scorecard.

---

## Next Steps

1. **Get user visual confirmation** of BAL3 position in build 72.
2. **If BAL3 → DD2 gap still too wide**: Refactor the `gapOverride` parameter in `repositionBALOutputBlocks` to support explicit zero (use sentinel -1 for default). Then call with `gapOverride = flowStep / 2` or similar.
3. **If BAL3 position is good**: Update `memory/project_task_completion_tracking.md` with session 4 results.
4. **DLY2 verification**: User previously reported DLY2 as "broken" — diagnostics show it rendering correctly. Confirm visually that the signal chain wires connect properly to DD2 at its new position (x=838).

---

## Key Technical Notes for Next Session

- **Two-pass layout**: First pass (line ~1809) computes initial positions. Second pass in `repositionBALOutputBlocks` lambda (line ~2208) overrides post-BAL block positions. Any x_axis hacks in the first pass are overwritten.
- **§L1 cap**: The `MAX_BAL_SPREAD` cap (currently 0) runs BEFORE the `bal3xpos += 1`. If MAX_BAL_SPREAD is changed, it could interact with the +1.
- **QRC cache**: Always bust with `rm -f build/release/qrc_qml.o qrc_qml.cpp` before building when QML files change. `build.sh` handles C++ changes fine but stale QRC has caused 4+ false "done" claims.
- **Diagnostic server**: TCP `localhost:47321`. Commands: `chain-positions`, `inspect-edit-panel`, `select-stomp N`, `screenshot PATH`.
