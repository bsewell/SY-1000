# Handoff — 2026-04-10 22:00

## Session Summary

Dropdown styling overhaul across all stompbox detail panels, plus a full visual review of ~30 panel screenshots against source code.

## Changes Made

### 1. SyComboBox Auto-Sizing (`qml/SyComboBox.qml`)
- Replaced fixed 380px width with TextMetrics-based auto-sizing
- Added `TextMetrics` element to measure widest option label at runtime
- Computed `comboWidth: maxOptionWidth + 36` (8px left pad + text + 20px arrow + 8px right pad)
- Added `showLabel` boolean property (default `true`): when `false`, internal 140px label area is hidden and `implicitWidth` becomes just `comboWidth`
- Added `labelWidth` property that returns 0 when `showLabel` is false

### 2. SyModeSelector (`qml/SyModeSelector.qml`)
- Added `showLabel: false` to embedded SyComboBox to prevent duplicate labels (SyModeSelector already provides its own external label)

### 3. FX Panels (`qml/Fx1Panel.qml`, `qml/Fx2Panel.qml`, `qml/Fx3Panel.qml`)
- Added `showLabel: false` to `fxTypeCombo` SyComboBox in all three FX panels (the Row already provides an external "FX TYPE" label)

## Builds Deployed

- **v2026.04.10.76** — Initial dropdown auto-sizing fix
- **v2026.04.10.77** — `showLabel: false` fix for SyModeSelector and FX TYPE rows

Both deployed to `/Applications/SY-1000FloorBoard.app`

## Unverified Items

- **Delay 2 TYPE selector row**: Screenshot shows it may be missing, but code in `Dd2Panel.qml:29-33` has the SyModeSelector. Asked user to confirm — **no response yet**. All three Delay panels have identical structure, so if it's missing only in Delay 2, it may be a rendering/timing issue.

## Known Issues

- **Delay 2 TYPE row (potential)**: May not be rendering despite being in code. Needs runtime confirmation.
- **Regression pattern**: The TYPE selector disappearing has happened before (user said "again"), suggesting this is a fragile area.

## Next Steps

1. **Confirm Delay 2 TYPE row**: User needs to click Delay 2 stompbox and verify if TYPE selector is visible or missing
2. **If missing**: Debug why `SyModeSelector` renders for Delay 1/3 but not Delay 2 — check if SyPanelBase Flickable is clipping it, or if `showLabel: false` change inside SyModeSelector causes a timing issue specific to Delay 2
3. **General**: All other panels (~30) reviewed against screenshots and look correct after the dropdown fixes

## Key Architecture Notes for Next Session

- **Two dropdown layout patterns**:
  1. **Inline in Flow** (DS, CS, AMP, RV): SyComboBox with `showLabel: true` sits in same Flow as knobs
  2. **Dedicated row** (Delay, Chorus, Divider, FX panels): SyModeSelector or manual Row provides external label, SyComboBox needs `showLabel: false`
- **SysEx timing** is hardware-constrained (20ms inter-message, 10ms receive polling, 3000ms spooler) — not software-optimizable
